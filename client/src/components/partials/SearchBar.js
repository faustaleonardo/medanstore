import React from 'react';

const Header = () => {
  return (
    <form>
      <input
        className="form-control"
        type="search"
        placeholder="Search"
        aria-label="Search"
      />
    </form>
  );
};

export default Header;
